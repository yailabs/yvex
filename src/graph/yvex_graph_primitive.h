/*
 * yvex_graph_primitive.h - graph primitive proof facts.
 *
 * Owner:
 *   src/graph
 *
 * Owns:
 *   graph primitive private include surface.
 *
 * Does not own:
 *   CLI parsing, command dispatch, rendering, stdout/stderr output,
 *   generation, eval, benchmark, or release decisions.
 *
 * Invariants:
 *   primitive proof code returns facts and never writes operator output.
 *
 * Boundary:
 *   primitive proof is not full graph runtime support.
 */
#ifndef YVEX_GRAPH_PRIMITIVE_H
#define YVEX_GRAPH_PRIMITIVE_H

#include "yvex_graph_report.h"

#endif
