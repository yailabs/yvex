/*
 * yvex_graph_plan.h - private graph-plan include boundary.
 *
 * Owner:
 *   src/graph
 *
 * Owns:
 *   internal include surface for graph plan construction.
 *
 * Does not own:
 *   CLI output, command parsing, rendering, runtime execution, generation,
 *   eval, benchmark, or release decisions.
 *
 * Invariants:
 *   this header only exposes graph-domain private state to graph files.
 *
 * Boundary:
 *   graph-plan declarations are not runtime readiness.
 */
#ifndef YVEX_GRAPH_PLAN_PRIVATE_H
#define YVEX_GRAPH_PLAN_PRIVATE_H

#include "yvex_graph_private.h"

#endif
