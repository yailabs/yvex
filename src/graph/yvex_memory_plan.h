/*
 * yvex_memory_plan.h - private memory-plan include boundary.
 *
 * Owner:
 *   src/graph
 *
 * Owns:
 *   internal include surface for graph memory-plan construction.
 *
 * Does not own:
 *   CLI output, command parsing, rendering, runtime execution, generation,
 *   eval, benchmark, or release decisions.
 *
 * Invariants:
 *   this header only exposes graph-domain private state to graph files.
 *
 * Boundary:
 *   memory-plan declarations are not runtime readiness.
 */
#ifndef YVEX_MEMORY_PLAN_PRIVATE_H
#define YVEX_MEMORY_PLAN_PRIVATE_H

#include "yvex_graph_private.h"

#endif
