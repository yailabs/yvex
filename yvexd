#!/usr/bin/env sh
set -eu

ROOT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
BIN="$ROOT_DIR/build/bin/yvexd"

if [ ! -x "$BIN" ]; then
  echo "yvexd: build/bin/yvexd not found or not executable" >&2
  echo "yvexd: run 'make' or 'make check' first" >&2
  exit 127
fi

exec "$BIN" "$@"
