# Backend API

The backend API is the boundary between CLORI planning/metadata surfaces and
execution adapters. Future backends may include a stub backend, llama.cpp
baseline adapter, CPU reference path, Metal and CUDA.

No inference implementation exists in this bootstrap delivery.
