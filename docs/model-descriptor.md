# Model Descriptor

The model descriptor is the normalized CLORI view of a model artifact. It should
capture model family, architecture, tensor inventory, quantization profile,
context limits, tokenizer posture, backend compatibility and memory estimates.

No inference implementation exists in this bootstrap delivery.
