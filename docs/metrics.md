# Metrics

CLORI metrics cover neural execution and benchmarking evidence: artifact size,
estimated memory, prefill latency, decode latency, tokens per second, stream
overhead, KV reuse, backend errors and degraded-mode posture.

Metrics are evidence. They are not YAI policy authority.

No inference implementation exists in this bootstrap delivery.
