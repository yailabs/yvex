# Benchmark Canon

No benchmark results exist yet.

Future benchmark results must be generated from reproducible runs. Reports must
separate:

- load
- prefill
- decode
- memory
- serving
- transport

No public number becomes canonical without a run artifact.
