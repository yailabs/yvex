# Test vectors

This directory is for stable, small, deterministic vectors used by unit, CLI,
runtime, parser, tokenizer, CUDA parity, and future generation tests.

Large model artifacts do not belong here.
